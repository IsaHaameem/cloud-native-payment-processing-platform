spring:
  application:
    name: analytics-service
  datasource:
    # Local default points at the compose Postgres (host port 55432). Overridden by
    # SPRING_DATASOURCE_* env vars in containers / production.
    url: jdbc:postgresql://localhost:55432/paymentflow
    username: paymentflow
    password: paymentflow
  jpa:
    hibernate:
      ddl-auto: validate        # Flyway owns the schema; Hibernate only validates it.
    open-in-view: false
    properties:
      hibernate:
        default_schema: analytics
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    schemas: analytics
    default-schema: analytics
    locations: classpath:db/migration
  kafka:
    # Local default points at the compose Kafka's host-published listener.
    bootstrap-servers: localhost:59092
    consumer:
      group-id: analytics-service-payment.events
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

server:
  port: 8093

paymentflow:
  kafka:
    payment-events-topic: payment.events
    # Matches payment.events' partition count (KafkaTopicConfig in payment-service) so
    # every partition can be consumed in parallel within this one consumer group.
    listener-concurrency: 3
    # M20.3: the request-log stream. Its own topic, group and concurrency rather than
    # sharing the payment listener's — this topic carries one message per API request
    # against one per payment event, and coupling them would let request-log backlog
    # delay payment aggregates. Concurrency matches the topic's 6 partitions.
    api-request-events-topic: api.request.events
    api-request-events-group-id: analytics-service-api.request.events
    api-request-listener-concurrency: 6
  request-log:
    # M20.3: partition maintenance. Hourly rather than daily — a daily job has exactly one
    # chance to succeed before the table it maintains starts rejecting writes.
    partition-interval-ms: 3600000
    partition-initial-delay-ms: 60000
    # M20.4 (§4.6, D116). The raw log keeps 30 days; api_usage_daily keeps everything.
    # Rollup runs before retention and retention refuses to drop a day the rollup has not
    # recorded — so a broken rollup costs disk, which is visible and recoverable, rather
    # than data nobody aggregated, which is not.
    retention-days: 30
    rollup-interval-ms: 3600000
    rollup-initial-delay-ms: 90000
    retention-interval-ms: 3600000
    # Staggered after the rollup's delay rather than equal to it: on a cold start the
    # ordering that matters is rollup-then-prune, and offsetting the first tick is cheaper
    # than coordinating them.
    retention-initial-delay-ms: 120000
  internal-context:
    # M19.6: no longer inert. The gateway routes /v1/analytics* here, so
    # InternalContextFilter is this service's actual (and only) authentication mechanism,
    # wired into the Spring Security chain by SecurityConfig (D124).
    secret: ${PAYMENTFLOW_INTERNAL_CONTEXT_SECRET:dev-only-insecure-shared-secret-change-me}
    max-clock-skew-seconds: 30

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when_authorized
  tracing:
    sampling:
      probability: 1.0
  otlp:
    metrics:
      export:
        # Prometheus (pull-based scrape) is this platform's metrics backend;
        # the OTel starter also auto-enables an OTLP metrics push exporter by
        # default, which has no receiver configured (Tempo only ingests traces)
        # and was found spamming connection-refused retries in every service's
        # logs during verification. Tracing export stays enabled below.
        enabled: false
  opentelemetry:
    tracing:
      export:
        otlp:
          # Local default points at the compose Tempo. Overridden by
          # MANAGEMENT_OPENTELEMETRY_TRACING_EXPORT_OTLP_ENDPOINT in containers.
          endpoint: http://localhost:4318/v1/traces

# Correlation/request ids (common-lib's filter) and trace/span ids (Micrometer
# Tracing, M13) surface on every log line.
logging:
  pattern:
    level: "%5p [correlationId=%X{correlationId:-},requestId=%X{requestId:-},traceId=%X{traceId:-},spanId=%X{spanId:-}]"
