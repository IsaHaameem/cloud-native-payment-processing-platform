{
  "name": "paymentflow",
  "version": "0.1.0",
  "description": "The official PaymentFlow API client for Node.js and TypeScript.",
  "license": "UNLICENSED",
  "//private": [
    "Still true, on purpose. `npm publish` refuses a private package, so this flag is the last",
    "deliberate step before a real release (see sdks/PUBLISHING.md), and SdkParityTest asserts",
    "it is here. Everything else a publish needs — the metadata below, the workflow, the",
    "provenance config — is already in place."
  ],
  "private": true,
  "type": "module",
  "keywords": [
    "paymentflow",
    "payments",
    "payment",
    "api",
    "sdk",
    "webhooks",
    "idempotency"
  ],
  "author": "Isa Hameem",
  "homepage": "https://github.com/IsaHaameem/cloud-native-payment-processing-platform/tree/main/sdks/node#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IsaHaameem/cloud-native-payment-processing-platform.git",
    "directory": "sdks/node"
  },
  "bugs": {
    "url": "https://github.com/IsaHaameem/cloud-native-payment-processing-platform/issues"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "engines": {
    "node": ">=18"
  },
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/esm/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "//sideEffects": [
    "Nothing in this package runs at import time - every module only declares. Saying so lets a",
    "bundler drop whatever an application does not reference, which matters because a webhook",
    "receiver importing only `constructEvent` has no reason to carry the transport, the eleven",
    "resource namespaces, or the generated model types."
  ],
  "sideEffects": false,
  "//test": [
    "Neither `node --test test/` nor `node --test \"test/**/*.test.mjs\"` runs this suite on every",
    "version the package supports - the directory form fails on Node 22+, the glob form fails on",
    "Node 20, which is what CI pins. scripts/run-tests.mjs enumerates the files and passes them",
    "explicitly, which is the one form all of them accept. Its header has the detail."
  ],
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && tsc -p tsconfig.esm.json && tsc -p tsconfig.cjs.json && node scripts/finalize-dual-build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:examples": "tsc -p tsconfig.examples.json --noEmit",
    "pretest": "npm run build",
    "test": "node scripts/run-tests.mjs",
    "verify": "npm run typecheck && npm test && npm run typecheck:examples"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.2",
    "@types/node": "^22.10.2"
  }
}
