# The SDK build runs on whatever JDK is present and targets Java 17 bytecode via
# `options.release` (see build.gradle.kts). No toolchain download is required.
org.gradle.caching=true
org.gradle.parallel=true
