/*
 * Builds the precompiled Kotlin-DSL convention plugins used by every module.
 * `kotlin-dsl` lets us author plugins as `*.gradle.kts` scripts under src/main/kotlin.
 */
plugins {
    `kotlin-dsl`
}
